<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.sch.smktelkom_mlg.project.xiirpl203132333.dapurnusantara">

    <application
        android:allowBackup="true"
        android:icon="@drawable/depan"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DetailActivity"
            android:label="@string/title_activity_detail"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".DetailMinumanActivity"
            android:label="@string/title_activity_detail_minuman"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".DetailTipsActivity"
            android:label="@string/title_activity_detail_tips"
            android:theme="@style/MyMaterialTheme"></activity>
    </application>

</manifest>